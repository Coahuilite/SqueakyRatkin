# MEMORY

## 当前耐久状态
- 项目为 RimWorld 1.6 模组 **鼠辈啁啾 / Squeaky Ratkin**；永久 `packageId` 为 `coahuilite.squeakyratkin`；**产品版本 0.3.3（进行中：csproj/About 已 bump，0.3.2 仅 GitHub prerelease `v0.3.2-pre1`，正式版按「跳过 0.3.2 正式发布」口径并入 0.3.3）**；最新正式发布 0.3.0（2026-08-21，GitHub 完整 + Workshop 页面级核验，Claim Pack [`docs/release_review/release-0.3.0-review-zh.md`](./docs/release_review/release-0.3.0-review-zh.md)）；prerelease 观察记录：[`docs/release_review/release-0.3.2-pre1-review-zh.md`](./docs/release_review/release-0.3.2-pre1-review-zh.md)。
- `0.2.4`（已发布）：GitHub 已发布核验（tag `v0.2.4`、Release CI `32044477840` success、资产 115 文件逐项核验）；Workshop 已上传且页面级核验完成（同一 item 3758115669、Updated 17 Aug 09:22、中英文案同步草稿）。Claim Pack：[`docs/release_review/release-0.2.4-review-zh.md`](./docs/release_review/release-0.2.4-review-zh.md)（含三项流程自我批评）。
- `0.2.3`：GitHub 已发布核验；Workshop 未上传。`0.2.1`/`0.2.2`：已发布，细节见各自 Claim Pack（`docs/release_review/`）。
- **包版本标识**：0.2.5 起三渠道（dev/steam/github）包内 `version.txt`（`SqueakyRatkin <版本>`/`build=<flavor>`/`commit=<sha>`，dev 带 `-dirty`），由 `stage-package.ps1` 统一写入；已发布的 0.2.4 各渠道包不含，重发与否由维护者定。
- 流程综合复盘（跨 0.2.x，含新增四项与通用原则）：[`docs/release_review/process-review-zh.md`](./docs/release_review/process-review-zh.md)。

## 权威入口
- **0.3.3 会话交接（未提交状态清单、已定案、发布分派、US 兼容与迁移裁决入口）**：[`docs/handoff-0.3.3-zh.md`](./docs/handoff-0.3.3-zh.md)；新会话先读 `AGENTS.md` → `MEMORY.md` → `TODO.md` → 该交接文档。
- 仓库导航：根 [`codemap.md`](./codemap.md)（纯汇总），具体目录读其 `codemap.md`；地图可能过期，源码与 `docs/` 现行合同权威。
- 架构/运行时/动作/发布边界：[`docs/project-architecture-contract.md`](./docs/project-architecture-contract.md)；设置 UI：[`docs/settings-ui-product-contract-zh.md`](./docs/settings-ui-product-contract-zh.md)；日志协议：[`docs/logging-protocol.md`](./docs/logging-protocol.md)。
- **0.3.x 实现架构（已接受，实施唯一入口）**：[`docs/0.3x-refactor-architecture-decision-zh.md`](./docs/0.3x-refactor-architecture-decision-zh.md)；规划输入（不覆盖合同）：[`docs/internal-universalization-design-note-zh.md`](./docs/internal-universalization-design-note-zh.md)。实现前将受影响结论提升进合同。
- VoicePack 作者约定（唯一正本，兼作 agent skill，**自包含：不依赖外部文档/模板，仅允许专用脚本**）：[`.github/skills/squeaky-voicepack-authoring/SKILL.md`](./.github/skills/squeaky-voicepack-authoring/SKILL.md)；脚手架 `scripts/new-voicepack.ps1`。Workshop 页面维护源：[`docs/steam-workshop-page-copy-draft.md`](./docs/steam-workshop-page-copy-draft.md)（含版本一致性/只读核验维护规则）；发布流程唯一入口：[`docs/release-runbook-zh.md`](./docs/release-runbook-zh.md)。

## 工程决定与交接
- `0.2.3`（已实现发布；分类=**优化/行为变更**）：默认音源策略改为 Fallback + 内置 Race Example 种子（纯 Vanilla 回退豚鼠 clip 占比高，婴幼儿动作集全部命中——"婴幼儿路由豚鼠叫"根因是默认态落回退层，非年龄路由）。迁移规则：`voicePackMode` 节点缺失（Scribe 省略默认值节点）→ Fallback + 启动链 `EnsureBuiltInRaceDefault()` 种子一次（`voicePackDefaultSeeded` 幂等）；显式模式与已有选择永不被覆盖。schema 2→3。
- `0.2.4`（已实现发布）：精神崩溃音 hook 收窄至 `MentalBreakWorker.TryStart`（MentalBreakDef 驱动唯一通道）；Biotech BabyFits（Giggling/Crying）属正常精神状态发作，不派发崩溃音。日志协议 append-only 扩展（`pawn=`/`pawn_id=` 尾随字段），由 `SqueakLogCharacterization` 锁定；后续 0.3.x 触发基线须锁定此 hook 语义，Crying/Giggling 动作兼容为 0.3.x 年龄域规划。
- `0.3.x` 架构决策（2026-08-18，分支 `0.3.x`；实施唯一入口 [`docs/0.3x-refactor-architecture-decision-zh.md`](./docs/0.3x-refactor-architecture-decision-zh.md)）：① 重构采纳「内核但薄」——决策逻辑（域键/池/链/fallback/年龄/Filter）进零 Verse `Kernel/` 编译集 + `tools/KernelCharacterization`（纯度门+单测+黄金语料），不拆程序集；吸收迁移事务性、`GetTargetCandidates` assembled-only 投影、BabyFits hook（`TryStartMentalState` 成功 postfix + `MentalFitDef` 反向 map）、黄金语料升级。② 动作门三方向比拼后采纳**受控开放**——`SqueakActionDef` 注册 + `SqueakCompat` 门面 + 全闸门复用 = 八道闸门链（含玩家总闸 `allowExternalActions` 默认开），0.4.x US 拆分与消费者同窗口落地；内核动作键出生即字符串（`ActionKey`，内置=枚举名，枚举留适配层设置/存档面）、srdiag v2 `action` 字段定型字符串键（0.3.1）。③ 0.3.1 定型 17 动作 ABI（2026-08-21 重排；Crying/Giggling append 15/16，内置表不列条目）与 Fallback 末端→profile→无声；内置表封闭于内置键；放弃信号（无第三方需求→退回封闭）。**0.3.0 等价验收第一原则：已发布功能=玩家必用，设置全项验证（唯一豁免=dev 隐藏功能）；发布门槛八面（A–H）+ 双轨发布 + 热修方案 a（决策文档 §5，三项已定案）**。
- **0.3.0 已发布（2026-08-21）**：GitHub 完整 + Workshop 页面级已核验（旧「内置 Example 默认启用」公告删除待维护者复核）；全部证据见 Claim Pack [`docs/release_review/release-0.3.0-review-zh.md`](./docs/release_review/release-0.3.0-review-zh.md) 与检查表 `docs/0.3x-release-gate-checklist-zh.md`；修复/卫生/打包纪律/发布决策定案提交 C12–C19。
- **0.3.1（2026-08-21 实施，离线面全部完成；提交链 C20–C32 在 `0.3.x` 领先 origin 18 个，未推送）**＝全分离 + race/年龄/fallback/彩蛋运行时 + XML ABI 定型 + 全部验证配置（决策文档 §5）：**内核自足**（`Kernel/` 物理自足：字符串键+`BuiltInActionKeys` 17 键+内核 `SelectionMode`+`ActionKey`/动作枚举入核；harness=`Kernel/*.cs`+漏斗纯文件显式链接；`SqueakActionModel` 出清单）；**race-aware**（`raceDefName` 必填 validator、catalog 域闸+试验名单替换开关、assembled-only UI 投影、事务性 Scribe 4/2（`SqueakSettingsMigration` 纯函数+失败阻断保存桥）、`AudioDomain` 域键端到端+`ComposeDomainKey` 删除、srdiag v2（fmt=2/SettingsOrigin/race/xenotype/tier/log-v2 once 域；v1 28 事件字节不变）；**年龄/fallback/彩蛋**（ageTag 多变体 exact→all-age 优先级、`SqueakLifeStageResolver` 直映 DevelopmentalStage、ComposeModulation 接 `voxPitch/voxVolume`、PackFallback tier（ctx.Domain 精确池）、`BuiltInFallbackCatalog` 15 键正式数据+`SqueakFallbackProfileStore`（Config 副本 SafeSaver 单写者，不经 WriteSettings）、Crying/Giggling append 15/16+`TryStartMentalState` 窄 hook（MentalFitDef 反向 map）、IsEgg 开关默认关（隐藏 Scribe `allowEasterEggSounds`，快照路由输入））；**验证配置**（漏斗纯逻辑提取（`SqueakActionPlan`/`SqueakTimingModel` 根纯文件+harness 链接）、双语料（corpus-0.3.0 3782 冻结 + corpus-0.3.1 10404 例 17 动作×彩蛋维度回放零 delta）、per-race 池隔离单测、`tools/ConfigCopyCharacterization` store 生命周期 7 场景、五处同步断言、v2 覆盖断言）。**实机面：维护者 dev 包（`dist/dev/SqueakyRatkin-dev-v0.3.1-6317005.zip`）实测（2026-08-22）：Off 模式内置表兜底（tier=vanilla）与模式往返、成人崩溃（MentalBreak）、婴儿 fits 静默零误报、多次重启迁移幂等隐式通过，Player.log 零 SR 错误；剩余低频项见 TODO 48。发布流程暂缓（维护者指示）。**
- **RimWorld 1.6 年龄体系事实基线（2026-08-21 RimSage 源码核验）**：判定链 `lifeStageAges`(minAge 阈值，race XML 数据) → `pawn.ageTracker.CurLifeStage` → `LifeStageDef.developmentalStage`（Baby/Child/Adult，**无 Toddler**）；Human 五段 HumanlikeBaby 0-3 / Child 3-9 / PreTeenager 9-13(Biotech) / Teenager 13-18 / Adult 18+；voxPitch 1.6/1.2/1.0。SR 一律经 CurLifeStage 直映，不自算年龄阈值；Toddler 为第三方预留桶，原版表不产生。
- **0.3.2（2026-08-23 提交 C34–C40 并发布 GitHub prerelease `v0.3.2-pre1`；Steam 阻断）＝功能调整 + XML ABI 固化**（SR 0.3.x UI 不变，原 UI 专项转 US 仓库；ABI 文档按「首个携带 0.3.1 XML ABI 的发布版本」表述）：① **玩家触发内容身份门控已实现**——`PlayerSelection`/`ActiveCommand` 要求 `Pawn.IsPlayerControlled`，`PlayerSelection` 另要求 `!Downed && Awake()`；gate 在 `NotifyExternal` 既有触发闸层（plan.Configured 后、TryTrigger 前）fail-closed 静默；纯标志 `IsPlayerInitiated`/`RequiresResponsivePawn` 进 `SqueakTriggerInvocation` + `TriggerInvocationRules` 单测；Verse 采样 out-of-scope；实机矩阵：**维护者已覆盖睡觉/非玩家 pawn/精神崩溃玩家 pawn 静默（2026-08-23）**，其余项见 TODO。**`playSound=false` 已定案过滤**（1.6 源码核验：全部 `playSound:false` 调用点均为程序性后续选择，patch 经 `__1` 位置注入过滤）。② **XML ABI 固化**——作者 XML 面自首个携带 0.3.1 XML ABI 的发布版本起公开冻结；`IsEgg` 纳入公开作者 ABI；**verify-local 第 10 项 `scripts/verify-voicepack-xml-abi.ps1`（示例 XML × validator × 作者指南三向对照 + C# 17 键 + 彩蛋测试包；作者指南标记源 2026-08-23 起 = `.github/skills/squeaky-voicepack-authoring/SKILL.md`）**；**作者指南 2026-08-23 统一为 SKILL.md 单一正文（人类可读优先，兼作 agent skill；原 docs 指南删除），并落地 `scripts/new-voicepack.ps1` 脚手架**；`action` 枚举→string 仍随动作门进 US。③ **彩蛋测试包 + 发配日志彩蛋适配 + dev 日志重排简化**：`dist/SqueakyRatkinEggTestVoices/`（gitignored 测试产物；eggtest，单 Select 蛋条目）；`ChainResult.IsEgg`→`SqueakSoundChoice.IsEgg`→装配器；成功路径只发一条 v2 `audio.route.selected` 明细（`sound tier egg suppressed_detail pawn pawn_id pawn_faction pawn_ctrl`，`pawn_ctrl=player|nonplayer` 二值、`pawn_faction=<FactionDef.defName>`；人类句 `Audio route: action -> sound (tier[, egg][, nonplayer])`），不再并列发 v1 `audio.dispatch.ok`（v1 28 事件与 registry 字节不动，characterization 双 flavor 已同步）。**彩蛋路由实机确认（2026-08-23 维护者）**：开关开 + 蛋包选中，Player.log 5 条 `Select -> SR_EggTest_Select_Select (race_pack, egg)` `egg=true`、同会话 `egg=false` 11 条对照、零 warning/error。**`pawn_faction`/`pawn_ctrl` 识别字段实机确认（2026-08-23，最新 dev 包）**：SR 41 行全 info；player 10 / nonplayer 26，PlayerColony 27 / Rakinia 9；PlayerColony 非可控 pawn 的 StateEvent/周期派发被正确标 nonplayer，无 Select nonplayer 泄漏。④ 0.3.x–1.0.0 路线见下一条。
- **Eat 触发粒度（2026-08-23 维护者决定并实施，承接 Steam 评论正面反馈）**：默认**保持 job 级派发**——父关时 `IsEating()` 即 `CurJob.def == JobDefOf.Ingest`，整个 Ingest job（取食 → 端食物走到餐桌 → 吞咽 → 收尾）都算 Eat（优先级高于 Move；`Eat=EachTime 144t` + 全局 `216t` ⇒ 约 3.6 s 现实时间一发，`v0.3.0` tag 与当前工作树数值一致）；**这是招牌手感，默认不得收窄**（0.4/US 重构同样适用）。**两级开关**（默认双 false；UI 在发声规则·频率组，属 0.3.x「SR UI 不变」口径的维护者点名单控件例外）：① 父 `eatOnlyDuringChewing` 开 = vanilla `IEatingDriver.GainingNutritionNow`（权威 = **正在摄入营养**而非「在嚼」；零营养成瘾品如烟卷/薄片不算；啤酒 `Nutrition 0.08`、仙馔 `0.2` 等带营养成瘾品已算）；② 子 `eatIncludeDrugs`（显示名「使用成瘾品」）开 = vanilla `ChewIngestible` toil（`JobDriver.CurToilString`，公开属性；零营养成瘾品也算），**toil 名本进程未确认时回落完整 job 级**（fail-open，绝不静默）；**父关时子项禁用且强制保持 false**（关闭父项即清零 + PostLoadInit 归一）。纯规则 `SqueakEatOccurrence`（`ResolveMode`/`AllowsOccurrence` + `ChewingOnlyDefault`/`IncludeDrugsDefault` + `ChewingToilDebugName="ChewIngestible"`，harness 链接 + 单测）。同步面：设置字段 ×2 / Scribe / fixture host 镜像 / 静态发布 / 双语 Keyed / 合同 §3 / SKILL §7 / 双语 CHANGELOG / Source + UI codemap / settings-ui 合同例外。**给 US/姊妹项目的可转述交接说明：[`docs/handoff-eat-occurrence-granularity-zh.md`](./docs/handoff-eat-occurrence-granularity-zh.md)**（vanilla 事实基线、静默 hole 复现、按钮需求规格、方案 A/B/C/D 取舍、§5.1 父+子规格、§7.1 改动面评估）。
- **0.3.x–1.0.0 路线重定（2026-08-22 维护者）**：0.3.x 期间 SR **UI 保持不变**、内核升级为 **US 通用状态**并**新开 US 仓库并行开发**（已确认 repo=`coahuilite/UniversalSqueaker`、packageId=`coahuilite.universalsqueaker`；命名空间/前缀/日志按推荐组合 `UniversalSqueaker`/`US_`/`usdiag`，显示名/许可待定；2026-08-22 暂不建仓，创建为外部操作另行授权）；**0.4 = US 与 SR 双仓同步上架创意工坊**，US 暂不作为 SR 依赖（独立共存），**共存策略已定案 = 可同时启用 + 服务域划分**（0.4 期间 SR 独占 Ratkin、US 0.4 不含 Ratkin 装配只服务其他种族、不写 `incompatibleWith`、双开矩阵验证同种族单响），SR 0.4 **只做 bugfix 并同步反馈 US**，动作门机制随 US 0.4 首版发布；**SR 下一版本直接从 0.4 跳到 1.0.0**，SR 退化为纯音频包（内容仓库），此时 **US 成为 SR 前置依赖**，1.0.0 = 作者 XML ABI 官方冻结版本（US 该时点版本号待定）；原 UI 专项全部转入 US 仓库开发面。**作者包 legacy 桥已定案（零迁移）**：US 接管版本起内置 `SqueakyRatkin.SqueakVoicePackDef` 薄继承类型 + `LegacyVoicePackSource` 单点 upcast，内核不引用 legacy 命名空间、随时可拆；**离线原型已完成（2026-08-22）**：`tools/LegacyBridgePrototype/`（真实 Verse API 编译期证明）+ `tools/LegacyBridgeHarness/`（共享桥源运行时语义），真实 Verse XML 加载/交叉引用步骤待维护者实机；0.4 期间 US 不得定义 `SqueakyRatkin.*` 类型（防与 SR DLL 同名类型 first-wins 冲突）。**US 兼容性检查（2026-08-23，只读审 US `0.4.x`）见 [`docs/us-sr-compatibility-check-zh.md`](./docs/us-sr-compatibility-check-zh.md)**：当前双开安全（US catalog 只承认 US 型 VoicePack，SR 包不可见）；但 US 自动装配的逃逸门只认自己程序集的 `CompProperties_Squeaker`，任一 US 型 Ratkin 包或 legacy 桥落地即出现 **Ratkin 双 comp → 双响**（两仓 15/16 patch 同目标、各推自己的 comp），须在 SR 退役窗口前解决（SR 侧 S1–S4：Ratkin 装配唯一写者、`SqueakyRatkin.CompProperties_Squeaker` 全名稳定、1.0 前置链、桥类型名重叠期归属）。**存量玩家约束下的迁移方案见 [`docs/us-sr-migration-plan-zh.md`](./docs/us-sr-migration-plan-zh.md)**：修复落 US 侧、SR 本窗口零运行时改动；顺序硬门 `U1 → US 型 Ratkin 包/桥 → SR 1.0 内容化`；已核实 **1.6 comp 不进存档**（`ThingWithComps` 按当前 def 重建 ⇒ comp 换型/退役对存档零影响）、依赖缺失=列表警告不崩档；退役靠长公告窗口（建议 4–8 周）+ 冻结 legacy 条目等替代路径。**身份归属（方案 §8，回应"SR 改 legacy + 新开 pack-only SR"）**：PackKey = `PackageIdNonUnique:defName` 且玩家设置存的就是它 ⇒ **packageId 应跟内容走**：pack-only 继承人继承 `coahuilite.squeakyratkin` 就地升级（PackKey/设置/订阅/第三方门零损失），legacy 独立线拿新 id 冻结；重复 packageId 被 RimWorld 忽略（`Log.Error`）、重复 defName 会被追加随机后缀（PackKey 失配）⇒ legacy 与新线不可同装；新线包应使用 US 原生 def 类型。（Q1–Q10 待裁决）
- **三命令发布契约与最小仪式（2026-09-13 定案，对齐 UniversalSqueaker）**：开发门 = `scripts/verify-local.ps1`（11 项）；发布面 = `scripts/check-pack-readiness.ps1 -RequireReleaseMetadata`（版本轴/仓库红线/暂存包/DLL 身份 + `[claim]` 快照，默认组合 verify-local）；隐私面 = `scripts/privacy-audit.ps1`（三向量独立 + 身份，CI 默认模式，push 前 `-FullHistory -PrePush`）。**push 前人工只有三步**（readiness + 隐私门 + 发布裁决），包内容清单不再手查；本地 commit 无需授权，远端操作需授权。评估与逐条裁决：[`docs/release_review/process-redundancy-review-zh.md`](./docs/release_review/process-redundancy-review-zh.md)。CI 已接线（`ci.yml` 跑 11 项 + 隐私门；`release.yml` 再加发布面门，SDK 对齐 10.0.x）；CI 改动推送后才实际生效。**2026-09-13 维护者裁决：V1 追认（本地 commit 免授权）、V3 接受（CI 加门 + SDK 对齐 10.0.x）、V2 先出专项方案不动 git**——重写方案与不可逆点见 [`docs/release_review/privacy-history-rewrite-plan-zh.md`](./docs/release_review/privacy-history-rewrite-plan-zh.md)。
- **本地一键校验（2026-08-23 扩至 11 项）**：`scripts/verify-local.ps1` = KernelCharacterization（纯度门+单测+双语料回放）/ SettingsFixtureGenerator 9 场景 / SqueakLogCharacterization 双 flavor / fixtures 零 delta / ConfigCopyCharacterization store 生命周期 / Dev+Steam 双 flavor 0 warning 构建 / **VoicePack XML ABI 一致性锁（第 10 项）** / **VoicePack 脚手架自检（第 11 项）**；`-PackDev`/`-PackSteam` 追加打包；`-NoRestore` 供离线缓存环境（默认仍常规 restore；**2026-09-13 实测坑**：csproj 浮动版本 `1.6.*` ⇒ 无网络时隐式 restore 必失败并覆写 `obj/project.assets.json`，此后 `--no-restore` 构建报 `MSB3644`；修复 = 以本机 NuGet 全局包缓存目录为 `--source` 做一次离线还原，所有门恢复 `-NoRestore`）；fail-fast + 单检查重跑命令。日常验证不需要 agent 会话。
- **已定案集（2026-08-20，决策文档 §2.3/§2.4）**：路由核心公理（路由=链唯一事实源；执行层只经输入/`ISoundGate` 注入面影响候选）+ 机械验证约束（新决策出生即纯、场景配通路或声明 out-of-scope、五处同步断言）；彩蛋 = 仅 `IsEgg` 标签（YAGNI）、加性池成员、开关默认关、0.3.1 XML ABI 同批落地。
- **归档分支 `0.2.4-FINAL` 基准 = dev@`8bd383d`（csproj 0.2.4）**：2026-08-20 建立并已推送远端（ref-only）；2026-08-23 tip `a9db9ab` 为唯一清理子提交（删除 `.slim` 工具残留，代码树不变）。
- **`.slim` 残留清理（2026-08-23 维护者）**：oh-my-opencode-slim 本地工具目录整目录加入 `.gitignore`；`main`/`dev`/`0.3.x`/`kiiro-experiment`/`0.2.4-FINAL` 各分支 tip 均已删除 `.slim/codemap.json`（C42 及分支清理提交；main 经 PR #25）。**历史提交与发布 tags（`v0.2.1`–`v0.3.2-pre1`）仍可达该文件（含本地绝对路径）；历史/tag 重写清理需维护者另行授权（force-push + tag 重建）。2026-09-13 实测（`privacy-audit.ps1 -FullHistory`，227 revision）：含本机展开路径的历史文件共 5 个——`.slim/codemap.json` + `AGENTS.md`/`CONTRIBUTING.md`/`MEMORY.md`/`TODO.md` 的旧版本（HEAD 均已净化）；凭据 0 命中、`PublishedFileId` 值 0 命中；身份面 3 个唯一身份（维护者两个显示名 + GitHub bot committer）全部 noreply。门以 `[known-debt]` 台账呈现，重写波及范围按 5 文件重估。**
- **发布流程边界**：runbook 为唯一入口；Steam 页面核验仅**玩家视角只读** filedetails（绝不尝试登录态/编辑界面，编辑=维护者人工）；workshop 草稿变更后必须核对版本号/下载链接/字符数/检查清单；包内容核验含 `version.txt`。维护者发布不使用 SteamCMD；Steam 页面核验 = **本地纪律不进 CI**（公开 API 只读轮询，2026-08-20 确认）。**Steam 打包纪律（2026-08-20 定案）**：Steam = 最终发布步 → `pack-steam` 干净树硬门（脏树 throw）+ `build-steam.ps1` 一步入口（Steam flavor 由构造保证）；三渠道 `stage-package` 断言 About.xml == csproj 版本。0.2.1 教训（LoadFolders 静默回归、codemap 泄漏、tag 重发 changelog、`merge -s ours`）已并入 runbook 与 process-review。
- Kiiro：实验分支不发布不 merge；通用化支持（标准 HAR `defName=Kiiro_Race`），无永久 adapter；Workshop 页面存在衍生作品限制，未经许可不得发布/宣传 Kiiro compat。
- `srdiag` characterization 护栏：`tools/SqueakLogCharacterization`（纯 net472 harness）：v1 = 28 事件双 flavor 字节锁定（`log-v1` once 域，冻结红线）；v2 = fmt=2 协议分支（`log-v2` once 域，覆盖反射断言）。扩展日志协议必须同步 characterization。
- 当前目标、开放行动、阻塞与明确延后项见 `TODO.md`。冷证据仅供历史冲突或明确请求，不能覆盖现行合同。
