# 更新日志

## 更新日志模板

本文件是 Squeaky Ratkin 的简体中文标准更新日志。

规则：
- 已发布条目按时间顺序记录，旧版本在前，新版本在后；未发布条目置顶。
- 标题使用本地发布时间：`[YYYY-MM-DD HH:MM UTC+8] 版本 X.Y.Z`。
- 顶端未发布条目可使用 `未发布 — X.Y.Z`；发布时替换为实际 UTC+8 时间。
- `创意工坊首次上传` 仅用于 Steam 创意工坊第一次上传；后续更新使用普通版本条目。
- 每条改动保持简短清楚，一条 bullet 只写一个改动。
- 需要时分为新增、变更、修复、打包、说明等小节。
- Bugfix 简要带过；只有影响玩家行为时才展开说明。
- 只有同时影响 GitHub 和 Steam 发布面时，才同时提到二者。
- 不记录未接受的计划；已接受但尚未发布的版本可使用上述明确“未发布”条目。
- 英文版需同步维护在 `docs/CHANGELOG.md`。

推荐条目格式：

```text
## [YYYY-MM-DD HH:MM UTC+8] 版本 X.Y.Z

一句话概述本次发布。

### 新增
- ...

### 变更
- ...

### 修复
- ...

### 打包
- ...
```

## 未发布 — 0.2.2

代码卫生与可读性版本；无玩家可见行为变化。

### 变更
- 内部日志模块重构为公开门面与内部协议模块；日志输出、事件 ID 与字段顺序不变。
- 触发组件的周期启动相位计算去重，社交任务标记列表集中。
- 移除一个无调用方的内部辅助函数。

### 打包
- `About.xml` 增加 `<modVersion>` 元数据，与 csproj 版本同步。

## [2026-08-16 01:54 UTC+8] 版本 0.2.1

Bug 修复版本：NewRatkinPlus fork 兼容、设置状态清理修复、重做的可拖动诊断面板。

### 修复
- 移除 `LoadFolders.xml` 的 `IfModActive="Solaris.RatkinRaceMod"` 包名门控——它会让改 packageId 的 NewRatkinPlus fork 静默失效；现在内容始终加载，发声组件按 `defName="Ratkin"` XPath 目标注入。
- 修复关闭模组设置后部分临时 UI 状态未正确重置的问题。

### 变更
- 诊断悬浮层重做：pawn 头上只显示带描边的单字符 `●` 标记；完整详情（含种族 `defName`）移入可拖动、不暂停游戏的诊断面板，带响应式两列网格、长文本换行、内容自适应高度（无滚动条）、屏幕边缘碰撞与双击 Esc 关闭。

## [2026-07-30 00:32 UTC+8] 版本 0.2.0

发布已接受的 0.2.0 功能范围。

### 新增
- 新增固定 15 个一次性动作，包括 Draft、Undraft、有边界的 Core 攻击、玩家下令 Equip 与 Mental Break 反馈。
- 新增玩家主动选择的独立 Race/Xenotype VoicePack、Off/Fallback/Remix 音源模式，以及精确 Xenotype `defName` 目标。
- 新增普通内置 Race Example 与可单独启用的 Extras Template；Template 仍是 Example 音频的唯一维护源。
- 新增即时设置表面：三个常规页、七击解锁的“开发与诊断”页、合并保存和关窗 flush。

### 变更
- 将 No-DLC Core 基线落实为端到端行为：Xenotype 是可选增量，Race 与原版回退持续可用。
- 将心情音高/音量调制置于运行时 SoundInfo factor，并保持动作、冷却、概率与距离由 XML 驱动。
- 将第三方音频资源根统一为 `<lowercase packageId>/<PackDef.defName>/<Action>/`。

### 修复
- 使发声器官拒绝与成功播放都消耗尝试冷却；Death 仍只豁免 Talking 概率门。
- 将攻击和装备触发限制在受支持的 Core/玩家命令路径。
- 不再让仅由 HAR 发现的原版 Xenotype 单独混入设置候选，并为不可用目标新增带确认的明确忘记操作。
- 让成功派发结果悬浮字跟随其记录开关，同时保持 DebugAction 菜单受 RimWorld 开发者模式门控。

### 破坏性变更
- 移除旧 `1.6/Sounds/Squeak/` 音频树以及 Pure/custom-only 音源模型。
- 第三方音频现在作为独立 VoicePack 提供，不再安装进主模。
- 旧 audio-selection 值不会自动迁移；本条目不声称其他设置是否保留。

### 打包
- 将内置与 Template Example 语音更新为 41 条公共领域素材，并统一规范为 22.05 kHz 单声道 Ogg Vorbis：Attack 3、Call 4、Death 2、Draft 3、Eat 2、Equip 2、Joy 3、MentalBreak 1、Move 3、Select 3、Sleep 3、Social 3、Undraft 3、Work 3、Wounded 3。
- staging 将 15 动作/41 OGG 的 Template 源镜像到内置 Example，并校验动作键、精确的各动作数量与 SHA-256 一致性。
- 本地测试仍仅使用 dist；Dev、Steam、GitHub 与共享 staging 脚本不会由构建部署到游戏。

## [2026-07-04 18:35 UTC+8] 版本 0.1.0

首个公开发布版本。

### 新增
- 加入完整的数据驱动鼠族啁啾系统。
- 加入空闲、进食、睡眠、受击、选中、移动、社交、娱乐与死亡触发。
- 加入动作冷却与每个 pawn 的全局冷却控制。
- 加入按心情运行时调制音高与音量。
- 加入原版豚鼠音效兜底。
- 加入自定义音频文件夹支持。
- 加入纯自定义音频模式。
- 加入距离音量衰减预设与自定义衰减区间。
- 加入倍速冷却补偿，用于降低高倍速下的触发密度。
- 加入按语言能力缩放触发频率。
- 加入死亡反馈。
- 加入心情调制工作台，支持精确输入、预设与预览。
- 加入 DebugAction 悬浮字开关与摄像机高度指示器。
- 加入英文与简体中文本地化。
- 加入 Dev、GitHub、Steam 构建 flavor。
- 加入本地测试、GitHub Release 与 Steam staging 打包脚本。

### 修复
- 修复启动 patch 绑定问题。
- 修复 XML range 解析问题。
- 修复 mote 定义红字。
- 修复高倍速音量变小。
- 清理发布包内容。

## [2026-07-05 08:14 UTC+8] 版本 0.1.1

发布构建排障工具与本地化体验修复版本。

### 变更
- 移除了面向玩家排障的 DebugAction 上错误的 Dev-only 编译门。
- DebugAction 现在会随 GitHub 与 Steam 构建一起发布。
- 悬浮字开关与摄像机高度指示器仍然需要 RimWorld 开发者模式与地图内状态。
- 新增游戏内模组名称与简介本地化支持。
- About.xml 保持为英文元数据 fallback。
- 将距离设置标题改为“距离音量衰减”。
- README 新增 3A 声明：AI 设计、AI 开发、AI 绘制。
- 自定义音频权利改由音频提供者自行声明，而不是由本模组统一声明。

### 修复
- 修复发布构建中 DebugAction 缺失的问题。
- 修复模组元数据本地化在语言系统未初始化时触发的翻译红字。
- 移除 release DLL 中的本机调试路径信息。

## [2026-07-05 08:14 UTC+8] 创意工坊首次上传

鼠辈啁啾首次上传至 Steam 创意工坊。

### 包含
- 包含版本 0.1.0 的全部功能。
- 包含版本 0.1.1 的发布构建 DebugAction 修复。
- 包含游戏内模组名称与简介本地化支持。
- 包含最终用于创意工坊上传的 Steam flavor 包。

### 说明
- 本模组通过引用使用原版豚鼠音效作为兜底。
- 不重新分发任何原版音频资产。
- 后续如加入自定义音频文件，其许可由对应音频提供者自行声明。
