# 0.3.0 发布门槛检查表（A–H 八面执行状态）

> 状态：**八面全绿（2026-08-20 实机面完成后落表）**。发布门槛达成；三项发布决策已定案、修复与文档已提交推送（C12–C14）；发布执行阻塞 = merge `0.3.x → dev` 授权 + 渠道发布授权。
> 依据：决策文档 §5「0.3.0 发布门槛」。每面 = 保证 + 证据；全绿 = 发布门槛。

| 面 | 保证 | 证据（已执行） | 状态 |
| --- | --- | --- | --- |
| A 听感 | 同 pawn/动作/心情/设置 → 同 SoundDef | 内核语义层：3782 例黄金语料（S1-S5 + F03-F07 fixture 驱动）生成+回放零 delta（C4/C7）；等价评审 14 行对照（C5）；**实机（2026-08-20）**：维护者听感抽样通过 + Player.log 87 次 `audio.dispatch.ok` 覆盖 `SR_OfficialExample_Race_*`（Fallback/Remix tier）与 `SR_*`（Off 内置回退）两面 | 绿 |
| B 时机 | 间隔/概率/冷却/全局冷却/启动相位/倍速 | TimingModel 与 CompSqueaker 行为代码零改动（v0.2.4→HEAD diff 审计：仅枚举移动）；等价评审 §3 不换面项；**实机（2026-08-20）**：触发节奏正常，日志 13 条 `trigger.outcome.summary`（最大单窗 dispatched=52）、0 条 `trigger.attempt.failed`；6 条 `audio.dispatch.no_sound`（每动作各 1 次，v1 既有事件，维护者未复现 → **按瞬态关闭**） | 绿 |
| C 设置/存档 | 0.2.4 设置读→写字节一致；无迁移执行；UI 行集合相同 | fixture 9 场景（含损坏修复）load→save 字节稳定（SettingsFixtureGenerator）；schema 未 bump（3/1）、零迁移执行；**实机（2026-08-20）**：维护者手测 C 面通过（四页/保存/footer/flush 正常，catalog 无刷新失败、无 PackRejected） | 绿 |
| D 日志 | v1 28 事件/字段序/once key/human 文案零变化 | SqueakLogCharacterization 双 flavor 全绿（日志文件未动，v0.2.4→HEAD 零差异） | 绿 |
| E 距离/调制/人口/vocal | 全零改动 | v0.2.4→HEAD diff：涉及文件（CompSqueaker 行为/SqueakVocalCapability/SqueakPeriodicPopulation/SqueakSoundAvailability/SqueakyRatkinSettings）零改动（仅枚举移动）；后置修复中 `CollectKnownSounds` 恢复 `_Preview` 排除为**回归恢复**（对齐 v0.2.4 语义） | 绿（审计） |
| F Def/内容 | `SR_*` 键、Example 音频、stage 镜像不变 | v0.2.4→HEAD：`1.6/` 零文件改动；`About.xml` 仅 `<modVersion>` 0.2.4→0.3.0（版本 bump C9，内容零改动）；`SR_*` 键源（SqueakActionDefinitions.AudioKey）未动；stage-package Template↔built-in SHA256 镜像校验通过（dev 包 116 文件） | 绿 |
| G 性能 | 触发路径无劣化（内核持平或优于旧 `ChoosePack`） | 代码级分配论证：旧 ChoosePack 每调用 `Where().ToList()` ×2 + LINQ 委托；新 SelectTier = 手写循环 + 单 valid List + 仅 sound 级一个 playable List，分配更少（等价评审 §4）；后置卫生再减一次权重求和。实机 dev 计数对比**未做（可选）**，接受论证 | 绿（论证） |
| H 依赖/降级 | No-DLC、Biotech dormant、HAR 缺失、Harmony 版本 | No-DLC 编译基线：主模组 Dev/Steam flavor 0 error（后置修复后 **0 warning**）；Biotech 门 `ModsConfig.BiotechActive` 分支保留（语料 F07 dormant 语义锁定）；**实机（2026-08-20）**：No-DLC 启动与触发由维护者独立验证成功（D1）；HAR 缺失 = 硬依赖预期（mod 列表报缺失、不崩溃）；Harmony 引用未动 | 绿 |

## 实机执行记录（2026-08-20，维护者回机 + 日志核验）

1. A/B：dev 包 `SqueakyRatkin-dev-v0.3.0-67028b8-dirty.zip` 实装，听感抽样 + 触发节奏通过；Player.log 核验：`mod.start.identity` = dev build 0.3.0+67028b88…、`mod.start.ready` count=37、87 派发、0 错误类事件（`rebuild_failed`/`refresh_failed`/`PackRejected`/`TargetRejected`/`hook.*unavailable` 全零 → BuildFallback 全程未触发）。
2. C：四页设置/保存/关闭 flush 手测通过；开发者页使用痕迹（`devtools.camera_indicator.changed`）。
3. H：No-DLC 独立验证成功；当前 Player.log 为 Biotech 活跃会话（D1 启动日志已被覆盖，以其独立验证为准）。
4. G：可选 dev 计数对比未执行，接受代码级论证（非阻塞）。
5. **三项发布决策已定案（2026-08-20）**：① 双轨 = dev 打包产物试用、不设 prerelease tag（维护者自选渠道分发，观察 2 周起）；② 措辞 = 内核重构 + 拆分预告（不提名，草案见决策文档 §5）；③ 热修 = `vX.Y.Z-hotfixN`（下个 z 合并）。发布执行仍阻塞：merge 授权 + 渠道发布授权。

## 后置修复（2026-08-20，已提交 C12）

`BuildFallback` 恢复种子内置表（修静音回归）；`CollectKnownSounds` 恢复 `_Preview` 排除；`KernelGate` 统一取 `ctx.Production`；删 `TotalWeight` 死计算；KernelCharacterization 41→43 断言（失败路径 2 条）。复验：三 harness 全绿（语料 3782 零 delta）、Dev/Steam 双 flavor 0 warning/0 error、dev 包重打包通过。另：Steam 打包纪律（`pack-steam` 干净树硬门 + `stage-package` About==csproj 断言 + `build-dev`/`build-steam` 入口）与决策文档 §2.3/§2.4 已提交（C12–C14）。

## 已锁定证据链（提交）

C1 `41ca953` fixture 9 场景 → C2 `efc08a0` 枚举提取 + ActionKey → C3 `e48d38f` Kernel 骨架 → C4 `fb9ab50` KernelCharacterization 41 断言 + 语料 1622 例 → C5 `ea5fd2c` 等价评审 → C6 `0893cd0` resolver 接内核 + 旧路径删除 → C7 `7e2c3f5` fixture 驱动语料 3782 例 → C8 `5c86315` 检查表（离线面绿）→ C9 `1b42a04` 版本 bump 0.3.0 → C10 `e6a1ed7` zip 打包脚本化 → `5b51c6a` handoff → `67028b8` MEMORY/TODO 同步（八面全绿）→ C12 `0b0fa48` 错误路径修复 → C13 `402d4be` Steam 打包纪律 → C14 `2e5fc74` 决策文档/检查表全绿。`origin/0.3.x` 已推送同步；`0.2.4-FINAL` 留档分支已推送（dev@`8bd383d`）；工作树干净。
