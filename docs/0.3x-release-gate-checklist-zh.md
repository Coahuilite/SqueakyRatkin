# 0.3.0 发布门槛检查表（A–H 八面执行状态）

> 状态：离线可验证面已完成（0.3.0 实施阶段）；实机面待维护者回机执行后全绿方可推 Steam。
> 依据：决策文档 §5「0.3.0 发布门槛」。每面 = 保证 + 证据；全绿 = 发布门槛。

| 面 | 保证 | 证据（已执行/待办） | 状态 |
| --- | --- | --- | --- |
| A 听感 | 同 pawn/动作/心情/设置 → 同 SoundDef | 内核语义层：3782 例黄金语料（S1-S5 + F03-F07 fixture 驱动）生成+回放零 delta（C4/C7）；等价评审 14 行对照（C5）。**待办（实机）**：游戏内 0.2.4 vs 0.3.0 同场景听感/选择统计对比 | 部分（语义层绿；实机待办） |
| B 时机 | 间隔/概率/冷却/全局冷却/启动相位/倍速 | TimingModel 与 CompSqueaker 行为代码零改动（v0.2.4→HEAD diff 审计：CompSqueaker 仅枚举移动）；等价评审 §3 不换面项。**待办（实机）**：0.2.4 vs 0.3.0 触发节奏观察/日志统计 | 审计绿；实机待办 |
| C 设置/存档 | 0.2.4 设置读→写字节一致；无迁移执行；UI 行集合相同 | fixture 9 场景（含损坏修复）load→save 字节稳定（SettingsFixtureGenerator，链接 0.3.0 代码 = 0.2.4 序列化面零改动）；schema 未 bump（3/1）、零迁移执行。**待办（实机）**：UI 快照断言（race 行 == {Ratkin}、catalog 非装配域条目 == 0） | fixture 绿；UI 实机待办 |
| D 日志 | v1 28 事件/字段序/once key/human 文案零变化 | SqueakLogCharacterization 双 flavor 全绿（日志文件未动，v0.2.4→HEAD 零差异） | 绿 |
| E 距离/调制/人口/vocal | 全零改动 | v0.2.4→HEAD diff：涉及文件（CompSqueaker 行为/SqueakVocalCapability/SqueakPeriodicPopulation/SqueakSoundAvailability/SqueakyRatkinSettings）零改动（仅枚举移动） | 绿（审计） |
| F Def/内容 | `SR_*` 键、Example 音频、stage 镜像不变 | v0.2.4→HEAD：`1.6/`、`About/` 零文件改动；`SR_*` 键源（SqueakActionDefinitions.AudioKey）未动 | 绿 |
| G 性能 | 触发路径无劣化（内核持平或优于旧 `ChoosePack`） | 代码级分配论证：旧 ChoosePack 每调用 `Where().ToList()` ×2（pack 级 + sound 级各一）+ LINQ 委托；新 SelectTier = 手写循环 + 单 valid List + 仅 sound 级一个 playable List，**分配更少**（等价评审 §4）。**待办（实机）**：可选 dev 计数对比 | 论证绿；实机对比可选 |
| H 依赖/降级 | No-DLC、Biotech dormant、HAR 缺失、Harmony 版本 | No-DLC 编译基线：主模组 Dev/Steam flavor 0 error；Biotech 门 `ModsConfig.BiotechActive` 分支保留（0.3.0 语料 F07 dormant 语义锁定）；HAR 反射非依赖未动；Harmony 引用未动。**待办（实机）**：受控 DLC 全关基线实测 | 编译绿；实机待办 |

## 实机待办清单（维护者回机执行）

1. 0.2.4（或 v0.2.4 tag 构建）与 0.3.0 同存档/同设置各跑 10-15 分钟：听感抽样对比 + resolver 统计对比（A/B 面）
2. 设置 UI 快照：race 行集合 == {Ratkin}、catalog 非装配域条目 == 0（C 面 UI 断言）
3. 受控 DLC 全关基线（H 面）：No-DLC + Biotech 关闭 + HAR 缺失三态启动与触发
4. 可选：dev 计数性能对比（G 面）
5. 三项发布决策定案（① 双轨 prerelease ② 发布措辞 ③ 0.3.0.x 热修路线）→ 双轨发布执行（决策文档 §5 发布门槛小节）

## 已锁定证据链（提交）

C1 `41ca953` fixture 9 场景 + 生成器 → C2 `efc08a0` 枚举提取 + ActionKey → C3 `e48d38f` Kernel 骨架 → C4 `fb9ab50` KernelCharacterization 41 断言 + 语料 1622 例 → C5 `ea5fd2c` 等价评审 → C6 `0893cd0` resolver 接内核 + 旧路径删除 → C7 `7e2c3f5` fixture 驱动语料 3782 例。分支 `0.3.x` 本地领先 origin 7 提交（未推送）。
